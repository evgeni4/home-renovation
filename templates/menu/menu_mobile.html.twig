<!--  mobile header area -->
<div class="mobile-header-area d-block d-lg-none">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-6">
                <!-- logo -->
                <div class="logo">
                    <a href="{{ path('home') }}">
                        <img src="{{ asset('assets/images/logo.png') }}" class="img-fluid" alt="">
                    </a>
                </div>
            </div>
            <div class="col-6 d-flex align-items-center justify-content-end">

                <!-- mobile menu -->
                <div class="mobile-navigation-icon" id="mobile-menu-trigger">
                    <i></i>
                </div>
            </div>
        </div>
    </div>
</div>
<!--  End of mobile header area -->
<div class="offcanvas-mobile-menu d-block d-lg-none" id="mobile-menu-overlay">
    <a href="javascript:void(0)" class="offcanvas-menu-close" id="mobile-menu-close-trigger">
        <i class="fa fa-times"></i>
    </a>

    <div class="offcanvas-wrapper">

        <div class="offcanvas-inner-content">
            <nav class="offcanvas-navigation">

                <ul>
                    <li><a href="/">Главная</a> </li>
                    <li><a href="{{ path('app_our_services') }}">Наши сервисы</a> </li>
                    <li><a href="{{ path('app_calculator') }}">Калькулятор стоимости</a> </li>
                    <li><a href="{{ path('app_about') }}">О нас</a> </li>
                    <li><a href="{{ path('app_contacts') }}">Связаться с нами</a> </li>
                    <li><a href="{{ path('app_testimonials') }}">Отзывы</a> </li>
                    {% if app.user %}
                        <li>  <a href="{{ path('app_logout') }}">Выйти</a></li>

                            <style>
                                .avatars{
                                    width: 50px;
                                    border-radius: 50%;
                                }
                            </style>
                            <img class="avatars" src="{{ asset('uploads/team/'~app.user.avatar) }}" alt="">
                            <strong>{{ app.user.fullName|upper }}</strong>
                    {% endif %}
                </ul>
            </nav>

            <div class="offcanvas-widget-area">
                <div class="off-canvas-contact-widget">
                    <div class="header-contact-info">
                        <ul class="header-contact-info__list">
                            <li><i class="ion-android-phone-portrait"></i> <a>{{ settingsAll.oneContactSettings.phone }}</a></li>
                            <li><i class="ion-android-mail"></i> <a >{{ settingsAll.oneContactSettings.address }}</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>